# Host filesystem path to index
HOST_PATH=/home

# Scan roots inside container (comma-separated)
SCAN_ROOTS=/data

# Root name for multi-mount setups
ROOT_NAME=data

# Stability window in seconds (skip recently modified files)
STABILITY_SEC=30

# Batch size for indexing (Meilisearch handles large batches well)
BATCH_SIZE=10000

# Meilisearch configuration
MEILISEARCH_DATA_PATH=./data/meilisearch
MEILISEARCH_PORT=7700

# Meilisearch environment (development or production)
# In production, you MUST set a master key
MEILI_ENV=development
# MEILI_MASTER_KEY=your-secure-master-key-at-least-16-chars

# API settings
API_PORT=8080
WEB_PORT=8081
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=500

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO